---
description: Run test suite
allowed-tools: Bash
---

Run the project test suite:

```bash
python3 -m pytest tests/ -v
```

If `tests/` does not exist yet, create it. Report results and show details for any failures.
