# Claude Code Project Configuration

@AGENTS.md

## Claude Code Specific

- Use `/test` to run the test suite
- Use `/progress` to update PROGRESS.md after completing tasks
- Connector development: use the `connector-developer` subagent
- Code review: use the `code-reviewer` subagent
