---
files: ["**/*.py", "scripts/**", "**/requirements*.txt", "**/*.yml", "**/*.yaml"]
---

# Python Conventions

- Always use `pip3` and `python3` in commands—never `pip` or `python`
- Run tests: `python3 -m pytest tests/ -v`
- Run scripts: `python3 scripts/script_name.py`
- Virtual env: `python3 -m venv venv`
